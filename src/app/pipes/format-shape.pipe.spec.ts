import { FormatShapePipe } from './format-shape.pipe';

describe('FormatShapePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatShapePipe();
    expect(pipe).toBeTruthy();
  });
});
